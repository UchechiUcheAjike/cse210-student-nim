import random

# TODO: Define the Board class here

#The program must have Board class in addition to what is already provided.
class Board:
    #The Board class must have one attribute called _piles (a list of numbers)
     _piles = ["1", "2", "3", "4","5"]

     #The Board class must have three public methods called apply, is_empty and to_string.
     def apply(self):
         pass

     def is_empty(self):
         pass

     def to_string(self):
         pass

     def __prepare(self):
         pass

         #The Board class must have one private method called _prepare.

